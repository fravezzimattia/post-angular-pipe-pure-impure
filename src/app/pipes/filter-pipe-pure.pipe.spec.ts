import { FilterPipePurePipe } from './filter-pipe-pure.pipe';

describe('FilterPipePurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipePurePipe();
    expect(pipe).toBeTruthy();
  });
});
