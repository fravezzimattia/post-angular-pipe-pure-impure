import { FilterPipeImpurePipe } from './filter-pipe-impure.pipe';

describe('FilterPipeImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
