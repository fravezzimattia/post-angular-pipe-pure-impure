import { CustomPipePurePipe } from './custom-pipe-pure.pipe';

describe('CustomPipePurePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipePurePipe();
    expect(pipe).toBeTruthy();
  });
});
